<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <title>Release Dashboard Login</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="">
    <meta name="author" content="">

    <!-- Le styles -->
    <link href="/bootstrap/css/bootstrap.css" rel="stylesheet">
    <link href="/bootstrap/css/bootstrap-responsive.css" rel="stylesheet">
    <style>
      body {
        padding-top: 60px; /* 60px to make the container go all the way to the bottom of the topbar */
      }
    </style>
    <!-- Le HTML5 shim, for IE6-8 support of HTML5 elements -->
    <!--[if lt IE 9]>
      <script src="//html5shim.googlecode.com/svn/trunk/html5.js"></script>
    <![endif]-->

  </head>

  <body>

    <div class="navbar navbar-fixed-top">
      <div class="navbar-inner">
        <div class="container">
          <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </a>
          <a class="brand" href="#">Release Dashboard <%= current_version %></a>
          <div class="nav-collapse">
            <ul class="nav">
            </ul>
          </div><!--/.nav-collapse -->
        </div>
      </div>
    </div>

    <div class="container">
      <h2>Please log in <small>Use your <a href="https://<%= jira_host %>" target="__rdashboard"><%= jira_host %></a> credentials</small></h2>
      <div>
        <% if @login_error %>
        <div class="alert alert-error">The username/password you entered was not recognized by <a href="https://<%= jira_host %>" target="__rdashboard"><%= jira_host %></a>. Please try again.</div>
        <% end %>
        <form method="post" action="/login">
          <% ['Username', 'Password'].each do |field_name| %>
            <div class="row">
              <div class="span1" style="margin-top:5px"><%= field_name %></div>
              <div class="span2"><input type="<%= field_name == 'Password' ? 'password' : 'text' %>" name="<%= field_name.downcase %>" style="width:150px"/></div>
              <div class="span1 help" style="padding-top:5px" rel="popover" data-original-title="Your Jira <%= field_name.downcase %>" data-content="Please type in the same <%= field_name.downcase %> that you use when logging in to https://<%= jira_host %>">
                <i class="icon-question-sign">&nbsp;</i>
              </div>
            </div>
          <% end %>
          <% if params[:dev] %>
          <div class="row">
            <div class="span2 offset1"><input type="checkbox" name="dev" checked="true"/> Developer mode</div>
          </div>
          <% end %>
          <div class="row">
            <div class="span1 offset1"><input type="submit" value="Log in" class="btn-primary btn-large"/></div>
          </div>
        </form>
      </div>
    </div> <!-- /container -->

    <!-- Le javascript
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
    <script src="/js/jquery.min.js"></script>
    <script src="/bootstrap/js/bootstrap.min.js"></script>
    <script>
      $('.help').popover();
    </script>
  </body>
</html>